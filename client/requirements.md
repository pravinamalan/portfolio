## Packages
(none needed)

## Notes
Uses existing shadcn/ui + framer-motion already installed.
All API calls use credentials: "include" and validate with Zod schemas from @shared/routes.
SEO: each route sets document.title + meta description via a small hook.
No stock images required (all visuals are abstract UI mockups).
